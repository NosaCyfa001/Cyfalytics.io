// Node.js/Express example
import WebSocket from 'ws';

const wss = new WebSocket.Server({ port: 8080 });

wss.on('connection', (ws) => {
  // Send sales updates whenever data changes
  pollSalesData((newData) => {
    ws.send(JSON.stringify(newData));
  });
});